# C-EASY-2：指针  题解 

特别提示：下划线部分及特别注释部分使用了ai工具



## Part 1 指针与结构体

### 指针

1. 如何在C语言中定义指针变量？指针变量的大小是固定的吗？其大小与什么有关？

   **定义指针变量需在变量前加上*号，表示此变量是指向某一数据类型的指针（如char \*p表示“p是一个指向char类型的指针”，int\*q表示“q是一个指向int类型的指针”）。指针大小固定，<u>其大小与平台有关：64位是8字节，32位是4字节</u>。**

   

2. 代码输出什么？

   **应输出“20 10”。**

   **第一个很简单：原理是计算机在printf的时候会先查找X的地址，而X的地址（p）指向的东西已经被修改成了20。**

   **第二个的考点是“数组名同时是数组首元素的地址”，所以*arr等于3，加一下得到4；++q使q的指向移动到数组的第二个元素上，取地址得到6。**

   

3. 野指针？

   **野指针是不知道指向什么内存的指针，有概率指向一些可怕的地方，通过指针访问或者修改这些地方时程序就会蠕动两下然后悲惨的死掉（呜呜）。避免方法如下：**

   1. **定义指针时先初始化为NULL或者直接申请一块内存。不要int \*p就跑了。**

   2. **free指针过后让它指向NULL。**

   3. **指针在数组里区区的移动的时候不要超出边界了。**

   4. **<u>避免函数返回局部变量的地址，因为它已经被清理掉啦</u>！**

      

4. 设计swap函数

   ```c
   void swap(int *a,int *b){
   		int c=*a;
   		*a=*b;
   		*b=c;
   }
   ```



### 结构体

1. 定义结构体？

   ```c
   typedef struct PerInfo{
       char name[10];
       char sex;
       int age;
       double height;
   }Per;
   ```

   

2. 结构体指针？

   **emm类似数组指针吧，是一个指向结构体类型数据的指针，不过可以直接用->访问结构体成员（以上一题的结构体为例做个演示）。**

   ```c
   Per p1;
   Per *a=&p1;//或者struct PerInfo *a=&p1
   (a->name)==(*a).name
   ```

   

3. 结构体内存对齐规则？

   **用我自己的话说......想象从第一个成员开始为结构体申请内存，前面所有成员所占的内存必须是后一个成员的整数倍。**

   **在Per结构体中，第一个name占8个字节，第二个sex纯良无害地占了1字节，第三个int占4字节......不好，9不是4的倍数！于是再空出来3个字节现在是16个，再填入height的8字节总共是24个。**

   **数据类型从大到小排可以省空间，Per的最小空间是8+8+4+1=21字节。**



## Part 2 链操作

（观看了黑马程序员的链表基础）



1. 链表和数组的存储？

   **数组储存的元素的地址是连续的，查找或修改某一元素很方便，但是增加或减少元素时会发生大面积的元素移动，计算机发出悲鸣！（分崩离析！）**

   **而链表使用数据和指针的结构实现环环相扣，非连续性的地址便于增减新元素，使得将链表搓圆捏扁成为可能。代价是性能的下降和占用内存的增多。**

   

2. 单向链表？

   **单向链表的结构大概是每一个节点中包含了：储存数据的数据域（data）和指向下一个节点的指针（next），最后一个节点的**

   **next是NULL（非循环链表情况下），另外还有一个头节点head不储存data，只有指向第一个储存数据的节点的next。**

   

3. 基本的链操作？

   **( 为了刚学链表的时候规范一点我用ai查了链表操作常用的英文函数名，初版代码为无辅助手写，使用ai检查并作数次修改)**

























